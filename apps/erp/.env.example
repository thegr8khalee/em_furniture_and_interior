# Operations console (Vercel, restricted). Everything VITE_-prefixed is compiled
# into the bundle — never put a secret here.

VITE_API_URL=http://localhost:5000/api
VITE_STOREFRONT_URL=http://localhost:5173
